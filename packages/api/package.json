{
  "name": "@ocome/api",
  "version": "0.0.1",
  "description": "Shared API client for web and mobile",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "tsc": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@ocome/types": "workspace:*"
  }
}
